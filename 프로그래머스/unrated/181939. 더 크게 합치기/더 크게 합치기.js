function solution(a, b) {
    var answer = 0;
    let sumA= String(a)+String(b)
    let sumB = String(b)+String(a)

   return answer = sumA > sumB ? Number(sumA) : Number(sumB)
   return answer = sumA === sumB ? Number(sumA) : null
    
    
}